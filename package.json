{
  "name": "billpay",
  "private": true,
  "version": "1.0.0",
  "description": "Billpay monorepo (frontend + backend)",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "pnpm --filter './*' run dev",
    "build": "pnpm --filter './*' run build",
    "lint": "pnpm --filter './*' run lint"
  }
}
